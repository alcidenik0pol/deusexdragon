In this folder, all assets can be used for the game.

generate metadata is used to generate the metadata for the assets.

adjust size is used to adjust the size of the assets after you change scale factor. no need to change anything else: just change scale factor and rerun adjust size.
